# Image 12: 多智能体系统

Use case: Chinese blog/article body illustration.
Asset type: one complete 16:9 Chinese handdrawn technical body illustration, final raster page.
Preferred final size: 1920x1080 if supported.

Page role: body illustration.
Create page 11/12 of a coherent illustration set.
Page number text exactly: 11 / 12
Title exactly: 多智能体系统
Subtitle exactly: 层次架构与平级架构两种协作模式
Archetype: left-right contrast
Main point: 多智能体系统有两种主流架构——层次架构（Orchestrator 分配任务给 Sub-agents）和平级架构（Peer-to-Peer 直接通信），A2A 和 MCP 协议是标准化通信基础。

Apply the deck style lock: see deck-style-lock.txt
Composition: Left side "层次架构": a small pyramid/tree structure with an orchestrator node at top (drawn as a small conductor figure with baton), three sub-agent nodes below connected by thin command lines, each sub-agent has a tiny specialty icon (搜索, 计算, 写作). Right side "平级架构": three equal-size nodes in a mesh network (drawn as small circles connected by bidirectional arrows), each node has a peer label. Between the two architectures, a subtle dashed divider line. Below both, a thin foundation strip labeled "A2A 协议 · MCP 协议" as the shared communication base. The two sides are visually balanced with equal weight. The composition occupies about 50% page width.

Required text only:
- 多智能体系统 (title)
- 层次架构与平级架构两种协作模式 (subtitle)
- 层次架构 (left heading)
- Orchestrator (top node)
- Sub-agent (child node label)
- 平级架构 (right heading)
- Agent A (peer)
- Agent B (peer)
- Agent C (peer)
- A2A 协议 (foundation)
- MCP 协议 (foundation)
- 11 / 12 (page number)

Avoid:
full-page border, yellow paper, beige paper, large central objects, oversized title, heavy bottom quote box, extra text, English, gibberish, watermark, crowded composition.
