# Image 09: RAG 工作流程

Use case: Chinese blog/article body illustration.
Asset type: one complete 16:9 Chinese handdrawn technical body illustration, final raster page.
Preferred final size: 1920x1080 if supported.

Page role: body illustration.
Create page 8/12 of a coherent illustration set.
Page number text exactly: 08 / 12
Title exactly: RAG 工作流程
Subtitle exactly: 检索增强生成：离线索引 + 在线查询
Archetype: horizontal process
Main point: RAG 包含两条流水线——离线索引（文档→分块→向量化→存储）和在线查询（问题→向量化→检索→增强提示→LLM生成答案）。

Apply the deck style lock: see deck-style-lock.txt
Composition: Two parallel horizontal pipelines (upper and lower) separated by a thin dashed centerline. Upper pipeline "离线索引": starts with a small document stack → Chunking (small scissors cutting pages) → Embedding (tiny matrix grid) → Vector DB (small database cylinder) at right. Lower pipeline "在线查询": starts with a question mark → Embedding (same tiny matrix) → 检索 (magnifying glass over DB) → 增强提示 (document+question merging into a prompt card) → LLM 生成 (small brain/processor) → 答案 (checkmark card). Slim arrows connect each stage. All object drawings are small and refined with pencil hatching. Each stage has a tiny pastel label. The dual pipeline is compact, about 60% page width. Leave large top and bottom margins.

Required text only:
- RAG 工作流程 (title)
- 检索增强生成：离线索引 + 在线查询 (subtitle)
- 离线索引 (pipeline label)
- 在线查询 (pipeline label)
- 文档 (stage)
- 分块 Chunking (stage)
- 向量化 Embedding (stage)
- 向量数据库 (stage)
- 用户问题 (stage)
- 检索 (stage)
- 增强提示 (stage)
- LLM 生成 (stage)
- 答案 (stage)
- 08 / 12 (page number)

Avoid:
full-page border, yellow paper, beige paper, large central objects, oversized title, heavy bottom quote box, extra text, English, gibberish, watermark, crowded composition.
